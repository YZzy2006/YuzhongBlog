# Backend AI endpoint (uses admin-configured AI provider: DeepSeek, MiMo, etc.)
BACKEND_URL=http://localhost:8080

# Fallback: direct API call (only needed if backend is not running)
XIAOMIMIMO_API_KEY=your_api_key_here
AI_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
AI_MODEL=mimo-v2.5
