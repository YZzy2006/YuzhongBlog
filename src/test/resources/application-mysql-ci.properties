# MySQL CI integration test profile
# Used by GitHub Actions CI with MySQL service container
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/yuzhong_blog_test?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA: create-drop for clean test isolation
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# JWT (test value)
jwt.secret=TestSecretKeyForCiIntegrationTests2026!@#$%^&*()_+MustBe256Bits
